問題發現:

1. 題目要求 PiCalculator 類別 必須使用 multi thread / parallel 來加速實作
   => 但這時會有一個問題: Thread Blocking 執行續阻塞
   如果說從最底層的實作函數地方使用到的 await 那一路從底層到頂層的過程都必須是 async/await 所以這會導致 同步問題
   即使是非同步也會被你寫成同步

2. 做過的任務不能再做
   => 如何確保這個任務沒有被做過?
   2.1 效率問題? 如何解決大資料情況下搜索困難? (O(N))
   2.2 如何解決競爭問題(race condition)?

3. 每秒自動刷新
   => 如果我本來執行後就會得到當下計算後的結果，我為甚麼要刷新?

如何設計 Concurrency 平行程式?
=> 使用 Queue 暫存所有併發進來的流量
